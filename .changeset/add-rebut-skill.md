---
---

Add the `rebut` skill: triage AI code-review findings on a pull request — verify each against the real code, fix the valid ones, refute false positives with evidence, and reply per thread from a separate review-bot identity. Reply-only; the human resolves the threads.

Release and version bump are deferred to the maintainer.
